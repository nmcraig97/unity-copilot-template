Last audit: 2026-05-05 (22 entries)
---
- **[2026-05-18]**: Added centurion-presence lesson to `editor-setup.instructions.md` (Lessons Learned): every scenario must include an `isCenturion: 1` player group or the loss-path Defeat condition is untestable.
- **[2026-05-06]**: Added Lesson E to `copilot-instructions.md` (Knowledge Maintenance § Overflow guard): `copilot-instructions.md` itself now has an explicit 200-line overflow guard — audit and migrate domain-specific sections to `.instructions.md` files when exceeded.
- **[2026-05-06]**: Added Lesson D to `copilot-instructions.md` (Code Style): integrity gates design principle — any evolving persistent collection needs quality floor, improvement gate, pool cap + pruning, and per-item quality metadata, all config-driven and defaulting to disabled.
- **[2026-05-06]**: Added Lesson C to `plan-review.instructions.md` (Completeness Checks #4): plans that introduce evolving persistent collections must be checked for all four integrity gate components before approval.
- **[2026-05-06]**: Added Lesson B to `editor-setup.instructions.md` (Lessons Learned): `ScriptableObject.CreateInstance<T>()` does not run C# field initializers for reference-type fields — assign all reference-type fields explicitly after `CreateInstance`.
- **[2026-05-06]**: Added Lesson A to `editor-setup.instructions.md` (Lessons Learned): `EditorApplication.update` manual coroutine loops do not recursively iterate child IEnumerators — flatten to synchronous or use EditorCoroutineUtility.
- **[2026-05-06]**: Added mandatory compile-error check rule to `copilot-instructions.md` (Copilot Workflow Preferences) and `plan-review.instructions.md` (Post-Implementation Code Review → When to Trigger). Root cause: previous session updated EventBus signatures without running `get_errors`, leaving 6 CS0123 errors undetected in `BattleEventLogger.cs` and 2 warnings in `GroupAgent.cs`.
- **[2026-05-05]**: Regression test — 2 issues found and fixed: (1) DR-CORE-003 event count wrong (41→48, 7 events missing: OnChargeAttackFired, OnFlankBonusTriggered, OnEnemyBehaviorTransition, OnEnemyMoraleRetreat, OnObjectiveProgress, OnObjectiveCompleted, OnStructureInteraction — likely added during BattleTestingInfrastructure work without a registry sync); (2) `overworld-generation.instructions.md` pattern named `DistanceToSegmentSq` but actual method is `SegmentDistanceSq` — fixed directly. All other Key Files and patterns verified accurate. Registry bumped r35→r36.
- **[2026-05-05]**: Filled 5 stubs in `copilot-instructions.md`: Interface Contracts (“none yet”), Input System Structure (real action maps), UI Theme (UI Toolkit rules), removed ScriptableObject Name Resolution and Stat Modifier System dead stubs, added Backlog section. Added Game Design Pillars section with full hero unit / incapacitation / merc replenishment / run structure / ambush design facts.
- **[2026-04-26]**: Generalized 7 project-specific lessons across `editor-setup.instructions.md` (5 lessons) and `documentation-sync.instructions.md` (1 lesson) — removed class/field/asset names, led each with the transferable principle. Moved BoxCollider lesson from Lessons Learned to Patterns in `editor-setup.instructions.md`. Added generalize step (step 3) to Knowledge Maintenance workflow in `copilot-instructions.md`.
- **[2026-04-26]**: Added 3 lessons to `overworld-generation.instructions.md`: (A) audit scene YAML for static `PrefabInstance` blocks before reading C# scripts when diagnosing wrong object placement; (B) Editor setup script GameObjects at scene root survive zone regeneration — delete all zone content they placed and rely on runtime dynamic spawning; (C) YAML surgery removing prefab instances does not clear serialized back-references in other MonoBehaviours — grep all removed component fileIDs and zero them out.
- **[2026-04-25]**: Created `overworld-generation.instructions.md` with 5 lessons: (1) Catmull-Rom spline doesn't reach room centers — use node-to-node straight-line clearance in `IsInRoomOrCorridor`; (2) `GenerateRoomWalls`/`GenerateCorridorWalls` conflict with fill walls — remove both when fill walls are active; (3) blanket `zoneParent` child destroy kills scene ground plane — use a dedicated `GeneratedContent` container; (4) `ClearOverworldPosition` must run after `PositionPlayerAtEntrance`, not before; (5) never rely on pre-placed scene objects for gameplay-critical geometry — generate everything procedurally.
- **[2026-04-21]**: Regression test — updated stale `[2026-04-16]` lesson in `editor-setup.instructions.md`: OverworldInitializer consumer list was missing `OverworldRunController` (added in Phase 2). All other 13 lessons and 5 patterns verified accurate against live codebase.
- **[2026-04-21]**: Added 3 lessons to `editor-setup.instructions.md` from Phase2_Batch5 plan review: (A) verify `[SerializeField]` exists before writing wiring steps — 8/19 findings were non-existent fields; (B) `EncounterConfig.troopTypeId` is a foreign key — cross-check all IDs against TroopTypeSO creation table; (C) SOs with array fields (`tiers[]`) are 1 asset with N entries, not N separate assets.
- **[2026-04-21]**: Added lesson to `documentation-sync.instructions.md`: EventBus events added to EventBus.cs during implementation were missing from DR-CORE-003 because the doc-sync instruction's applyTo glob doesn't fire on Assets/Scripts edits → update DR-CORE-003 event roster in the same task as any EventBus change.
- **[2026-04-21]**: Added lesson to `plan-review.instructions.md` Architectural Analysis: plan assigned TransitionTo/BattleConfig.Pending responsibility to a plain C# class (EventCardManager) which has no scene access → flag when plan assigns state-machine transition work to non-MonoBehaviour classes.
- **[2026-04-21]**: Added 2 lessons to `editor-setup.instructions.md`: (1) `var()` in UXML inline styles crashes play-mode test runner → use literal values in UXML, `var()` only in USS; (2) `CampPOI.ApplyFreeRefill` requires TroopTypeSO → use `maxGroupHP` directly in `HandlePOIInteract`.
- **[2026-04-21]**: Created `assembly-boundaries.instructions.md` with Core/Data cycle constraint, EventBus parameter rules, and lesson: Centurions.Core cannot reference Centurions.Data → EventBus events must be parameterless or primitive; Data-needing classes belong in Data assembly.
- **[2026-04-21]**: Added 2 lessons to `editor-setup.instructions.md`: (1) `GetComponentInParent` cached in `Awake` returns null in edit-mode test fixtures → wire via reflection in test setup; (2) `Has.One` is not a valid NUnit constraint → use `Is.EquivalentTo` or `Does.Contain`.
- **[2026-04-20]**: Updated `plan-review.instructions.md`: Added Review Session Cap (max 2 pre-impl sessions, stop if <3 new findings), Post-Implementation Code Review section (5-point checklist with test plan output for coverage gaps). Created `review-plan.prompt.md` and `review-code.prompt.md` for explicit phase triggering of pre- and post-implementation reviews.
- **[2026-04-20]**: Refined `plan-review.instructions.md`: Promoted Findings Persistence to blocking Step 0 (with explicit memory commands) and blocking Final Step (persist before editing). Added ≤10-line prompt cap for Explore subagents. Root cause: post-update validation showed 1/3 sessions skipped persistence and 1/3 used oversized Explore prompts.
- **[2026-04-18]**: Major update to `plan-review.instructions.md`: Added Mechanical Verification phase (assumption extraction → batch grep → internal consistency → removal safety) before architectural exploration. Added Findings Persistence rule (session memory). Added Architectural Analysis section with targeted Explore guidance. Added cross-batch modifications completeness check. Root cause: 4 review iterations on Phase2_Batch2 plan re-discovered overlapping findings due to broad Explore-first workflow and no inter-session memory.
- **[2026-04-15]**: Added lesson to `editor-setup.instructions.md`: UXML `visible: false` is invalid USS → use `display: none` and `DisplayStyle.Flex/None` in C#. Fixed in PostBattleSummary, BattleHUD, OverworldHUD, PauseMenu.
- **[2026-04-15]**: Created `editor-setup.instructions.md`. Lessons: SerializedObject scene persistence failure → YAML direct-write; UIDocument requires PanelSettings; UI-only scenes need Camera; UIDocument.visualTreeAsset setter triggers RecreateUI crash → use SerializedObject.
- **[2026-04-15]**: Deleted plan-refine ensemble (plan-refine, plan-benchmark, perf-reviewer, robust-reviewer, simple-reviewer agents). Ported valuable checks into `.github/instructions/plan-review.instructions.md`.
- **[2026-04-16]**: Added pattern to `editor-setup.instructions.md`: TroopGroup slot buffer caps group size — must increase when scaling encounter configs.
- **[2026-04-16]**: Added lesson to `editor-setup.instructions.md`: SO creation helpers silently skip existing .asset files → delete stale assets before re-running Batch5Setup.
- **[2026-04-16]**: Added lesson to `editor-setup.instructions.md`: Trigger BoxCollider + kinematic Rigidbody for group-level click selection on de-parented units.
- **[2026-04-16]**: Added lesson to `editor-setup.instructions.md`: OverworldInitializer must wire RunData to all consumers or interactions silently fail.
- **[2026-04-16]**: Added workflow preference to `copilot-instructions.md`: Always specify exact Unity menu paths, not vague script references.
